# Telegram Bot credentials
# Create a bot via @BotFather on Telegram and get the token
TELEGRAM_BOT_TOKEN=8803463856:AAFm-qChnYV4NjNAQ7EGdTX9gTUj9RgGBTo
TELEGRAM_CHAT_ID=8803463856

# SMTP credentials for email notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=${{ vars.GMAIL_SMTP_USERNAME }}
# The SMTP account password
SMTP_PASSWORD=${{ secrets.GMAIL_SMTP_PASSWORD }}
SMTP_FROM=${{ vars.GMAIL_SMTP_USERNAME }}k
SMTP_TO=${{ vars.GMAIL_SMTP_USERNAME }}k

# Optional: override log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
